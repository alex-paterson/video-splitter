# Backlog

- Memory file should log files modified/generated and why.
- At end write a second .memory.debug.md that our agent is to ignore but it includes the chain of tools called and reasoning.
- At the end when the orchestrator has finished publishing, it should tell the user a prompt they can use to re-generate the video for next time (e.g. give all the options and content descriptions with the hope the orchestrator would repeat the same steps and arguments next time) - this is output video by ouptut video, not one prompt to create multiple videos.
