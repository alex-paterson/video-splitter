# Backlog

- Ability to remove or silence inappropriate phrases (profanity, names, etc.) in a rendered clip/compilation — make it a new option whenever rendering a video.

- Make sure that agent can generate SEGMENT ideas or COMPILATION ideas or BOTH and render BOTH. Add new tools and agents if necessary.

- Make sure I can specify a maximum length for any video that's being planned for and those ones are discarded. Make sure if an idea is discarded by an agent then the orchestrator knows it needs to regenerate a new idea or a variation.

- Make the backend expose an endpoint that the frontend connect to to continousouly receive all the events from the backend (they still log also).

- Make it so if the agent finds that a transcript file already exists, it doesnt regenerate it UNLESS TOLD TO.
